# Bubble Sort

## Algorithm name

Bubble Sort

## Author

Victor Manuel Montini (2026-03-23)

## Algorithm description

Bubble sort is a simple sorting algorithm that repeatedly steps through the list, compares adjacent elements and swaps them if they are in the wrong order. The pass through the list is repeated until the list is sorted. The algorithm gets its name because smaller elements gradually "bubble" to the top of the list with each iteration. An optimization stops early if no swap occurs during a pass, indicating the array is already sorted.

## Model used to create the base code

claude-sonnet-4-6

### Prompt

```
Write a C function called bubble_sort that receives an integer array `number_array` and its length `array_length`, and sorts the array in ascending order in-place using the bubble sort algorithm. Include an early-exit optimization. Use descriptive English variable names and comments.
```

## Obfuscated version

manually obfuscated
